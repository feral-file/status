# Phase-2 handoff — V3 chain rollout COMPLETE; what remains and in what order

*Written 2026-09-04 for the next agent. Owner: Brandon. Self-contained. Plan +
history: `ops/cdn-retirement-phase2.md`. OpenSea incident (read BEFORE touching
anything OpenSea-facing): `ops/opensea-metadata-path-incident.md`.*

## Where things stand (all verified, with receipts)

**DONE — the full V3 arc:**
- Step 0: population rebuilt (11,517 tokens, 104 pin units); every contract
  chain-audited (V3 2,341 needs_fix = census exactly; crystalline 9,048/9,048;
  Truth 0 — its 128 census-CDN rows are the filum `alternativePreviewURI`
  overlay, out of scope). Records in `step0/`.
- Step 1: all 104 CDN units (40.2 GB, 74k files) mirrored from the origin bucket
  and pinned on prod-02, ff-gateway byte-verified (`step1/dir_cids.csv`,
  104/104 verified=yes). prod-02 ~727 GB of 900 GB after everything.
- Step 2: DB exports for both V4 contracts (9,944 rows) — `artworks.metadata
  .ipfs_cid` = `<dirCID>/<tokenId>` path form on V4, both already chain-aligned;
  Truth needs NOTHING in-phase.
- Step 3 regen: V3 2,341 docs + crystalline 9,048 docs, byte-preserving, only
  media keys changed — independently verified (reverse-substitution byte proof
  per doc; V3 media 1:1 content-proven 1,782/1,782 pairs; crystalline offline
  correspondence 9,048/9,048 ×2 + samples; `step3/VERIFICATION.md`). All new
  docs pinned on prod-02 (`step3/staging_roots.csv` + crystalline new dir
  `QmNP6RC7Z5DRV8sQsmRgssQS3GP77BUCAK2UV4q94WfiMv`, pin confirmed).
- **V3 CHAIN ROLLOUT COMPLETE (2026-09-03):** `run-contracts.sh` finished all
  six contracts — per-contract full check green: Material Wonderland 108/108,
  Gray Matter 171/171, BOOM TOWN 182/182, Chain Reaction 600/600, Peer to Peer
  611/611, I KNOW 669/669 = **2,341/2,341 `updateArtworkEditionIPFSCid` txs
  confirmed on chain** (`tools/v2-metadata-regen/runs/<contract>/run.log`,
  done.txt now 23 contracts incl. the 17 goal-2 ones).

**NOT DONE — in recommended order:**

1. **HARD RULE: DO NOT dispatch OpenSea metadata refresh** for the V3 tokens
   (or crystalline after its switch) until OpenSea confirms the metadata-path
   fix — refresh is the one action known to trigger the re-bucketing.
   (Delist monitoring on OpenSea's side is being handled by OpenSea directly —
   Brandon has the thread with Ryan; no scan needed from us. The scanner
   `tools/opensea-delist-scan.py` stays available if spot-checks are ever
   wanted.)
2. **crystalline (V4_2) — the one remaining tx.** Everything is staged:
   new dir `QmNP6RC7…` pinned; old dir `QmY67Gq1514Zj1yWtHxoHeoVj8FpFLM5ZNSNQejjirxKTo`.
   Sequence: rerun `tools/phase2-step0/check-base-uri.py` (now records owner())
   → fill `tools/update-token-uri/v4-base-uri.config.example.json` →
   `v4-base-uri.mjs preflight → tx → vault sign → broadcast` → then
   `tools/phase2-step3/gen-v4-sql.py --old-dir QmY67Gq… --new-dir QmNP6RC7…`
   → back-office SQL (path-form UPDATEs, WHERE-pinned).
3. **V3 DB align.** The API serves V3 docs from `artworks.metadata.ipfs_cid`;
   the chain now points at the NEW doc CIDs, DB still at the old ones. Run
   `step2/export-v3-tokens.sql` (back-office, expect ~2,476 rows) → generate
   UPDATEs from `step3/updates_0x*.csv` old→new mapping, artworks keyed by
   id = token_id, WHERE pinned to the old value (check the export first to see
   whether V3 stores bare CIDs or path form — the SQL generator does not exist
   yet; model it on `gen-v4-sql.py` / phase-1 `gen-sql.py`).
4. **Reference rows.** `tools/phase2-step2/gen-reference-sql.py` with the V3
   export + `crystalline_db_export.csv` + `step1/dir_cids.csv` → ipfs_reference
   upserts (conflicts/unmapped go to review CSVs, never clobbered).
5. **Close-out measurement.** `tools/pin-referenced` rerun with a fresh DB
   export (referenced set grew), census on prod-02, status page rebuild —
   ETH `dependent` should drop from 11,389 to ~overlay-only (filum 128 + TW 39).
   OpenSea refresh per contract remains GATED on item 1.
6. **Unpin backlog** (superseded HLS + old V2 metadata dirs + now the old V3
   docs and crystalline old dir) — only after the census confirms nothing
   references them; re-derive the reference set first.

## Parallel/pending tracks (not phase-2 blocking)

- **OpenSea/Ryan thread**: restore IE's 15 tokens into the verified collection,
  remove duplicate, get the per-collection metadata-source list. Also confirm
  the bypass from FF API access logs (prod-01, refresh window 2026-09-01).
- **Background delists**: 11 untouched tokens on 6 contracts are item-delisted
  for unrelated reasons (~4-5% of sampled controls). Full-population sweep with
  the same scanner + its own appeal track.
- **Overlay/product decisions**: filum 128 (a 7-attribute crossorigin patch
  would make the IPFS version whole — needs artist sign-off) + Ten
  Whistlegraphs 39. Packaged in `ops/cdn-retirement-phase2.md` § Pending
  decisions. Status page should split overlay-only from doc-level CDN.
- **Scheduled archive-probe** with Sean's two independent checks (both nodes on
  kubo 0.43 now); ff-pin-1 pre-upgrade DO snapshot deletable after ~09-05.
- **agentic-workflows**: upstream issue for the 21 contract-held Tezos works
  (census refetch still drops them; rescan mode A is the workaround).
- **#3435 checkpoint comment**: the whole phase-2 arc since 9/1 is unreported
  on the issue; a checkpoint should cover V3 completion + the OpenSea incident.

## Gotchas learned this phase (so the next agent doesn't relearn them)

- OpenSea per-token metadata source varies (FF API vs direct tokenURI) —
  grouping comes from API-injected collection_name/uuid; direct reads lose it.
- `artworks.metadata.ipfs_cid` on V4 is a PATH (`<dirCID>/<tokenId>`); V2 uses
  `swaps.ipfs_cid`; V3 serves from `artworks.metadata.ipfs_cid` (form TBC from
  export).
- The CDN rewrites `generated_images/<name>?variant=<v>` →
  `generated_images/<v>/<name>` (folded into paths in the crystalline regen).
- V3 has no `trustee()` getter — authorization proven via per-token eth_call
  dry-runs; sender was `0xbeb9f810…` (same vault key as goal-2).
- kubo API: pin/add streams errors after the 200 header (check for `"Pins"` in
  the body); big uploads go per-file/batched into MFS, never one giant POST;
  `sort | head -1` under pipefail dies on SIGPIPE for big lists.
- Public RPCs are DNS-blocked on this network — use Infura; Etag parsing:
  strip `W/` prefix then quotes only (`.strip('"W/')` eats trailing W's).
